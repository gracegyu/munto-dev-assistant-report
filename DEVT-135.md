# munto-dev-assistant에 오케스트레이션 기반의 무중단 에이전트 루프 체계 구현


| 항목 | 내용 |
| --- | --- |
| 배경 | Munto 개발팀은 사내 AI 툴 munto-dev-assistant로 에이전트 기반 개발을 하고 있다. 그러나 작업이 채팅 세션과 사용자 개입에 크게 묶여 있어, 동일 품질 기준을 유지하려면 사용자가 반복해서 지시하고 검증해야 한다. 해당 이슈를 통해 그 부담을 줄이고, 사용자 부재 상황에서도 맥락이 끊기지 않게 이어 가는 절차를 레포에 고정하고자 한다. |
| 현재 문제점 | • **작업 연속성**: 에이전트 작업이 채팅 세션 및 사용자 개입에 의존하여, 무인 상태에서 동일 맥락이 장시간 유지되기 어렵다.<br>• **완료 판정의 오류**: lint·test 등 검증 통과 여부가 확정되기 전에 작업이 종료되거나 "완료"로 처리되어, 대화상의 완료 선언과 레포의 실제 품질이 어긋날 수 있다.<br>• **표준화 부재**: 성공·실패·중단을 판단할 검증 명령·중단 조건·금지 작업이 팀 단일 규약으로 고정되어 있지 않아, 동일 요청이라도 처리 결과의 기준이 달라질 수 있다.<br>• **무인 실행 주체의 부재**: "장시간 무인으로 돌린다"는 요구에 맞는 반복 실행·검증·재시도를 실제로 수행하는 구성요소가 없거나 역할이 정해져 있지 않아, 문서·스킬만 있고 요구 동작이 재현되지 않을 수 있다. |
| 개선 방향 | 무인으로 요청된 작업을 끝까지 수행하기 위해, **오케스트레이션 구성요소**를 도입한다. 해당 구성요소는 **요청 식별 정보·작업 브랜치·검증 명령 목록·중단 조건(반복 횟수·시간·동일 오류 등)·개인 Slack 수신 설정**을 입력으로 받아, **코드 변경(또는 에이전트 호출) → 검증 실행 → 결과 판정**을 자동으로 반복한다.<br>완료(정상 종료)는 **합의된 검증 명령이 모두 성공**한 경우로만 정의하며, 그때까지는 **대화 종료나 부분 수정만으로 완료를 선언하지 않는다.** 검증 로그는 판정 근거로 보존한다. **검증 통과 판정은 에이전트의 자기 보고가 아니라, 오케스트레이터가 검증 명령의 exit code와 로그를 직접 파싱하여 결정한다.**<br>**정상 종료가 확정된 직후**, 사전에 등록한 **개인 Slack**으로 요청 요약·통과한 검증·브랜치·커밋·PR 링크(해당 시)를 전송한다. **비정상 종료** 시 Slack으로 종료 원인을 통지한다. <br>본 오케스트레이터는 외부 워크플로우 프레임워크 도입 없이, **Claude Code를 에이전트 런타임으로 그대로 활용**하는 얇은 구성으로 설계한다. 루프 형태는 "에이전트 호출 → 검증 명령 실행 → 결과 판정 → 재시도/종료"의 단순 반복으로 한정한다(복잡한 다단계 토폴로지가 필요해지는 시점에 재검토).<br>**실행 주체: GitHub Actions** 를 채택한다. `workflow_dispatch` 또는 이슈/코멘트 트리거로 작업을 개시하며, 매 실행마다 격리된 러너에서 수행한다. 사용자 머신 가동 여부와 무관하게 동작하며, 시간·CPU 한도와 감사 로그·시크릿 관리가 기본 제공된다. 로컬 데몬/사내 VM은 "사용자 머신 의존" 또는 "운영 부담"으로 무인 요구사항과 충돌하므로 제외한다. |
| 고려요소 | • **비용·한도:** 토큰·API·러너 CPU·시간에 **상한**을 두고, 상한 도달 시 **비정상 종료**로 처리한다. 무한 재시도는 **비용·안전** 측면에서 허용하지 않는다.<br>• **보안·권한:** 프로덕션 DB·시크릿·무승인 배포 등 **고위험 작업은 기본 금지 사항**으로 설정한다.  <br>• **알림 :** 작업 종료 시 사전에 입력한 개인 Slack을 통해 정상 종료/비정상 종료 여부를 발송한다. |
| 기대효과 | • **무인 연속 작업:** 사용자 개입 없이 **동일 작업 맥락**이 유지되어, 요청 단위 작업을 **끝까지 수행**할 수 있는 기반이 마련된다.<br>• **가시성:** 작업 종료 시 **개인 Slack**으로 결과가 전달되어, **요청자가** 완료 여부와 근거를 확인할 수 있다. |


## comment by 김세현

개발팀 (@김범진 @김도연 @홍진영 @김세현 @전규현)

해당 이슈의 기존 설계는 작업 한계에 도달했을 경우 비정상 종료로 끝나도록 구성되어 있었습니다. 
이 문제를 해결하기 위해, 한계 상황에서 Github Actions 러너를 중단하고, 개발 담당자의 Slack 피드백을 받아 멈춘 지점부터 비동기로 재개하는 구조의 Onepager를 작성했습니다. 리뷰 부탁드립니다. 
 
https://app.notion.com/p/munto/munto-dev-assistant-Stateful-376e2bc7639d802a9624fde4b76c2057

## comment by 전규현

@김세현 에스컬레이션 기반 Stateful 루프 SRS 1차 리뷰했습니다. 방향성 먼저 봤습니다.

방향 자체는 맞습니다. 실패 임계치에서 *그냥 죽던* 기존 설계의 빈자리를 "State 저장 → 사람 에스컬레이션 → Slack 재개"로 메우는 발상은 TO-BE §4.9.4 BLOCKER 핸드오프·§4.8 변경 위임과 같은 줄기고, 러너를 끊어 대기 비용을 0으로 만드는 것도 합리적입니다. 문제 정의와 핵심 통찰은 좋습니다.

다만 오해 하나 정리: "OpenClaw 같은 기성 서비스를 쓰는 것"이 원래 계획이 아니었습니다. OpenClaw는 우리 TO-BE의 *가칭·미구축* 요소였고, §4.9.8은 처음부터 GitHub Actions 기반 *자체 구축*을 권장했습니다. 즉 구축 방향 자체는 이탈이 아닙니다. 단, 권장 형태는 "외부 프레임워크 없이 얇게(GitHub Actions + Slack + Jira)"였습니다.

가장 큰 보완 (필수)

- 구현 착수 전 **GitHub-native 1차 대안 평가**가 필요합니다. 수동 승인 일시정지는 Environments required reviewers, 사람 지시 채널은 이슈/PR 코멘트 트리거, 상태 보존은 Actions artifact로 상당 부분 대체 가능합니다. Lambda 중계·S3·멱등성이 *왜 native로 안 되는지*를 1페이지로 먼저 정리해 주세요.
- 제어·지시가 **Slack 단독**이면 §4.7.9(레포=단일 진실)·§4.8(BLOCKER→Jira)와 충돌합니다. 에스컬레이션/사람 지시/재개 결과를 **해당 Jira 이슈에 자동 기록**하는 경로를 추가해 주세요.

반영 권장

- 24h 후 base→target 자동 `git merge`는 §4.9.6 안전 기본값과 긴장 관계입니다. "재개 시 사람이 최신 base 반영 후 재개"를 기본으로 두는 보수안을 권합니다.
- State 산출물은 이미 git 브랜치에 남으므로 S3 필요 범위를 최소화 검토(브랜치 state 파일/artifact 대안).
- 1차 구현은 단순 고정 임계치로 MVP화, Root Cause 조기 감지(Jaccard 등)는 실패 로그 축적 후 2차로.
- 재개 후에도 통과 판정=오케스트레이터(§4.9.3), 머지=사람(§4.9.5) 불변임을 본문에 명시.

멱등성·HMAC·즉시 Ack 같은 디테일은 신참치고 감각이 좋습니다. 다만 *가장 단순한 것부터 올리는* 순서를 권합니다.

상세: 본 코멘트 + Review 본문 참고.